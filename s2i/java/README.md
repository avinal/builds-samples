# Hello Spring Boot

A simple Spring Boot application for demonstrating S2I builds with OpenJDK 21.

To run locally:

    mvn clean spring-boot:run

    $ curl http://localhost:8080/
    Hello, World!
